import 'package:flutter/material.dart';

class FavoriteMealsTab extends StatelessWidget {
  const FavoriteMealsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
