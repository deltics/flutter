// Imports
import 'package:flutter/material.dart';

// Dart entry point
void main() {
  runApp(App());
}

// The App widget
class App extends StatelessWidget {
  Widget build(BuildContext ctx) {
    return MaterialApp(
      home: Text('Hello!'),
    );
  }
}
